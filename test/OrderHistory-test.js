import chai from 'chai'
const expect = chai.expect;
import OrderHistory from '../src/OrderHistory.js'

describe('OrderHistory', function() {
  let orderHistory;
  beforeEach(function() {
    orderHistory = new OrderHistory();
  });

it('should have default properties', function() {
    
  });
})